// Single source of the frontend version. Injected from package.json by Vite
// (see vite.config.ts `define`). The backend version comes separately from /meta.
export const APP_VERSION: string = __APP_VERSION__;
